<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.enpit_p12.sainttropez">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:theme="@style/App1Theme" ></activity>
        <activity android:name=".buyctivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SellActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".BuydecideActivity" />
        <activity android:name=".ChatActivity"
            android:theme="@style/App1Theme"></activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginMakeActivity" />
        <activity android:name=".NewMessageActivity" android:theme="@style/App1Theme">
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".ChatActivity"/>
        </activity>
    </application>

</manifest>